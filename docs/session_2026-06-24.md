# Session notes — 24 June 2026

## What was done today

### Performance benchmarking
- Compared Jun 22 (no pool) vs Jun 24 (pooled) live call logs
- Confirmed 18.5x TTS speedup: 4,269 ms → 231 ms warm TTS latency
- Total warm turn dropped from ~4,578 ms → ~600 ms
- Full numbers documented in `docs/latency_benchmarks.md`

### Step 9 — Redis concurrency gate (fully implemented and live tested)
- Created `utils/redis_gate.py` — two Lua scripts: atomic acquire on `/voice`, idempotent release on `/api/call-status`
- Added `CONCURRENCY_GATE_ENABLED`, `REDIS_URL`, `MAX_CONCURRENT_CALLS` to `config/__init__.py`
- Added `init_gate()` / `get_gate()` singleton to `orchestrator/factory.py`
- Wired gate check into `/voice` (admit or return busy TwiML) and gate release into `/api/call-status`
- Updated `run_server.py` startup banner and init sequence
- Verified live: `[GATE] Concurrency gate ready — max 5 concurrent calls` on startup
- Verified live: `[GATE] Released slot for CAd95f... (completed), active=1` after call end

### Documentation
- Created `docs/latency_benchmarks.md` — per-turn latency tables, Jun 22 vs Jun 24
- Created `docs/parity_09_redis_gate.md` — full step doc with Lua script explanation
- Updated `docs/PARITY_PORT_OVERVIEW.md` — Steps 7, 8, 9 status corrected; tech stack table updated
- Updated cold-start runbook — added Docker startup step, status callback URL to Twilio update command
- Updated memory files — TTS performance + LLM target

### API keys
- New ElevenLabs API key added to `.env` (new personal account — free tier)

---

## What is still left

### Step 10 — Test suite (next session)
- pytest setup: unit tests for governance interceptor, RAG search, connection pool
- Mock tests: STT queue, TTS synthesis, LLM response
- Smoke test: full call flow with mocked Twilio WebSocket

### Pending — needs a key or credential
| Item | Blocked on |
|---|---|
| OpenAI 4o + 4o-mini as primary LLM | Manager approval + paid API key |
| Azure Neural TTS fallback | Azure Speech key |
| Real Bedrock embeddings (semantic RAG) | AWS credentials (ACCESS_KEY + SECRET_KEY) |
| ElevenLabs paid voices (library voices) | Paid ElevenLabs account |

### Minor polish (no blocker)
- ElevenLabs pool warmup hits `/v1/models` which returns 401 on free tier — cosmetic only, synthesis works. Switch warmup endpoint to `/v1/user/subscription` to fix.
- System prompt: LLM occasionally says "I don't have any further questions for you" (should be "for you at this time" → "for you"). Minor phrasing cleanup.
- Step 10 will also add `active=0` gate validation to the smoke test.

---

## Current parity status

| Step | Feature | Status |
|---|---|---|
| 1 | Data models + Pydantic schemas | ✅ done |
| 2 | FastAPI + uvicorn migration | ✅ done |
| 3 | Orchestrator extraction + telephony split | ✅ done |
| 4 | Governance layer | ✅ done |
| 5 | Logging upgrade | ✅ done |
| 6 | Connection pooling (STT + TTS) | ✅ done — 18.5x TTS speedup confirmed |
| 7 | RAG (pgvector + mock embeddings) | ✅ done — 16 vectors live, RAG_ENABLED flag |
| 8 | LLM swap (Gemini behind flag, Groq active) | ✅ done |
| 9 | Redis concurrency gate | ✅ done — live tested |
| 10 | Test suite | next session |
