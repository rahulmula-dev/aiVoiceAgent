🏗 S4-11 IMPLEMENTATION BLUEPRINT

(For Your Exact Codebase)

🎯 Core Architectural Principle

We will:

Keep conversation_history exactly as-is (Gemini schema)

Add a parallel structured turn store

Add barge-in state handling in manager.py

Add classification + structured output in brain.py

Add continuation control logic in manager.py

Extend CRM logging payload

Add phrase regression guard

We will NOT break Gemini schema.

1️⃣ SESSION MODEL UPDATE

File: orchestrator/session.py

Add new fields inside Session:
structured_turns: List[Dict[str, Any]] = []
current_speaking_turn_id: Optional[int] = None
Structured Turn Schema (dict format)

Each turn must be stored like:

{
    "turn_id": int,
    "topic": Optional[str],
    "agent_response_status": "completed" | "interrupted" | "abandoned",
    "agent_partial_response": Optional[str],
    "barge_in_classification": Optional[str],  # NEW_TOPIC | SAME_TOPIC | AMBIGUOUS
    "is_multi_step": bool,
    "continuation_offered": bool,
    "timestamp": str
}

We are NOT modifying Gemini format.
We are adding a control layer.

2️⃣ TTS INTERRUPTION CAPTURE

File: tts/synthesizer.py

You must expose a method:

def stop_current_speech(call_id: str) -> str:
    """
    Stops TTS playback immediately.
    Returns last spoken partial text.
    """

It must:

Stop audio stream

Return last text chunk spoken

That returned text becomes:

agent_partial_response
3️⃣ MANAGER — CORE BARAGE-IN LOGIC

File: orchestrator/manager.py

Add method:

def handle_barge_in(self, call_id: str, caller_input: str):

Flow:

STEP A — Stop TTS + Mark Interrupted
partial_text = tts.stop_current_speech(call_id)

current_turn = session.structured_turns[-1]

current_turn["agent_response_status"] = "interrupted"
current_turn["agent_partial_response"] = partial_text
STEP B — Classify + Respond (Call Brain)

Call:

classification, response, is_multi_step = brain.generate_with_classification(
    session=session,
    caller_input=caller_input
)
STEP C — Update Interrupted Turn

If classification == "NEW_TOPIC":

current_turn["agent_response_status"] = "abandoned"

If classification == "SAME_TOPIC":
Keep interrupted (natural continuation)

Store:

current_turn["barge_in_classification"] = classification
STEP D — Create New Turn Entry

Append new structured turn:

{
    "turn_id": new_id,
    "topic": inferred_topic,
    "agent_response_status": "completed",
    "agent_partial_response": None,
    "barge_in_classification": None,
    "is_multi_step": is_multi_step,
    "continuation_offered": False
}
4️⃣ BRAIN UPDATE — FORCED STRUCTURED OUTPUT

File: orchestrator/brain.py

You MUST modify system prompt.

Add instruction:

When handling barge-in input, you MUST first classify it as:
NEW_TOPIC, SAME_TOPIC, or AMBIGUOUS.
Then provide a natural response.
Never ask procedural confirmation.
Never say "Should I continue from where I left off?"

Force JSON output:

{
  "classification": "NEW_TOPIC",
  "topic": "Scholarships",
  "response": "Scholarships are available...",
  "is_multi_step": false
}

Manager parses this.

If output is not valid JSON → retry once.

5️⃣ RETRIEVAL CONTROL (Very Important)

Inside manager before calling RAG:

if classification == "NEW_TOPIC":
    run_retrieval()
else:
    reuse_existing_context()

This prevents duplicate vector hits.

6️⃣ SOFT CONTINUATION LOGIC

Inside manager.py, AFTER generating response:

If:

previous turn["is_multi_step"] == True

classification == "NEW_TOPIC"

previous turn["continuation_offered"] == False

Append to response:

response += " I can also finish walking you through the remaining steps if that's helpful."

Then:

previous_turn["continuation_offered"] = True

Never repeat.

7️⃣ CRM EXTENSION

File: crm/client.py

Extend payload to include:

"structured_turns": session.structured_turns

OR flatten:

turn_id

status

classification

continuation_offered

partial_response

8️⃣ REGRESSION GUARD

Create test:

File: tests/test_barge_in_phrase_guard.py

def test_no_procedural_continuation_phrase():
    forbidden_phrase = "Should I continue from where I left off?"
    assert forbidden_phrase not in generated_response

Better: Scan entire output string.

This must fail CI if phrase appears.

9️⃣ STATE TRANSITION UPDATE

File: contracts/state.py

Add state:

INTERRUPTED = "interrupted"

Update transitions:

SPEAKING → INTERRUPTED → LISTENING
🔟 TEST CASES TO MANUALLY VERIFY

You must test:

Interrupt mid-sentence → TTS stops

New topic → no reference to old answer

Same topic clarification → continues naturally

Multi-step interruption → one-time soft offer

Phrase never appears

3 interruptions → memory preserved

CRM contains full metadata