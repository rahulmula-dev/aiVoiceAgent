# apiVersion: autoscaling/v2
# kind: HorizontalPodAutoscaler
# metadata:
#   name: cila-ai-agent-hpa
#   namespace: gd-college
# spec:
#   scaleTargetRef:
#     apiVersion: apps/v1
#     kind: Deployment
#     name: cila-ai-agent
#   minReplicas: 2  # PRD: 24x7 warm operation - No cold starts
#   maxReplicas: 10 # Scale up to 10 pods during peak admissions
#   metrics:
#   - type: Resource
#     resource:
#       name: cpu
#       target:
#         type: Utilization
#         averageUtilization: 60 # Scale when CPU hits 60%
  
#   # --- PRD REQUIREMENT: SCALE ON STT LATENCY / QUEUE LENGTH ---
#   # To implement scaling on non-resource metrics (latency/queue), 
#   # a Custom Metrics API or KEDA (Kubernetes Event-Driven Autoscaling) is required.
#   # 
#   # Example KEDA Scaler (Pseudo-code):
#   # - type: prometheus
#   #   metadata:
#   #     serverAddress: http://prometheus-server.monitoring.svc.cluster.local
#   #     metricName: avg_stt_latency_ms
#   #     threshold: '800'  # Scale if STT latency > 800ms
#   # ---
